/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.redd.it', 'upload.wikimedia.org', 'logos-world.net', 'preview.redd.it', 'images.crunchbase.com', 'cdn1.iconfinder.com']
    }
}

module.exports = nextConfig
