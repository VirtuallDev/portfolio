

const Navbar = () => (
    <header>
        <nav className="flex fixed top-0 w-full px-36 h-32 navbar-shadow">
            <ul className="flex items-center gap-4 w-full">
                
            </ul>
        </nav>
    </header>
)

export default Navbar;